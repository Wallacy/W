# Executable benchmark history

This directory is the immutable, content-addressed home for accepted
`executable-result` records. The index is intentionally empty until the W
Hello candidate is rerun from a clean committed HEAD. Local exploratory output
belongs under the ignored `benchmarks/results/` directory and is not history.
