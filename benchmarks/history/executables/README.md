# Executable benchmark history

This directory is the immutable, content-addressed home for accepted
`executable-result` records. Each indexed result is immutable and addressed by
the SHA-256 digest of its canonical bytes. Local exploratory output belongs
under the ignored `benchmarks/results/` directory and is not history; derived
best-known records are regenerated from every indexed result.
