REst like grapqhl

/restql

usando definição de classe em js

GET:
get (properties)
PUT
set (properties)
POST:
call functions
Options:
definição

//
trpc-openapi
//
Nome alternativo: restpc (já tem um restql) e considerando que a pessoa não vai fazer uma query rest em sí faz um certo sentido... uma nova linguagem de query irá exigir um novo parser etc, só padronização o que da pra fazer puramente em js/json/http seria otimo.
