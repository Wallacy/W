{
  "name": "tree-sitter-w",
  "version": "0.0.1",
  "description": "Candidate Tree-sitter grammar for the W language",
  "license": "MIT",
  "private": true,
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "queries",
    "src/**"
  ],
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "tree-sitter test",
    "parse:restaurant": "tree-sitter parse --grammar-path . --quiet --stat ../../examples/restaurant/*.w",
    "check": "npm run generate && npm test && npm run parse:restaurant"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.26.11"
  }
}
