{
  "name": "tree-sitter-w",
  "version": "0.0.1",
  "description": "Candidate Tree-sitter grammar for the W language",
  "license": "MIT",
  "private": true,
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "queries",
    "src/**"
  ],
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "tree-sitter test",
    "parse:restaurant": "tree-sitter parse --grammar-path . --quiet --stat ../../examples/restaurant/*.w",
    "parse:fixture": "tree-sitter parse --grammar-path . --quiet --stat ../vscode-w/fixtures/*.w",
    "check:design": "node ../check-design-examples.mjs",
    "check": "npm run generate && npm test && npm run parse:restaurant && npm run parse:fixture && npm run check:design"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.26.11"
  }
}
