{
  "name": "tree-sitter-w",
  "version": "0.0.1",
  "description": "Candidate Tree-sitter grammar for the W language",
  "license": "MIT",
  "private": true,
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "queries",
    "src/**"
  ],
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "tree-sitter test",
    "parse:restaurant": "tree-sitter parse --grammar-path . --quiet --stat ../../examples/restaurant/*.w",
    "corpus": "bun ../../corpus/runner.ts",
    "check": "npm run generate && npm test && npm run parse:restaurant && npm run corpus"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.26.11"
  }
}
