# AEG0 artifact index

| Artifact | Role |
|---|---|
| `bundle.json` | R1 bundle with current, reserved candidate, and adversarial variants |
| `study.json` | Host-only study contract and evidence boundary |
| `current.w` | Thin W witness for current controls |
| `candidate.txt` | Reserved comparison text |
| `adversarial.w` | Thin W witness for rejected routes |
| `oracle.test.mjs` | Deterministic host-oracle tests |

The canonical payload is `tooling/aeg0-app-essentials-gate-cases.json`.
